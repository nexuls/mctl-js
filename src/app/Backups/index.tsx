/**
 * Backups — placeholder until backup providers land (Phase 4).
 */

import { Placeholder } from "../shared.tsx";

export function Backups() {
	return (
		<Placeholder
			title="Backups"
			phase="Phase 4"
			note="Filesystem and cloud backup providers, scheduling, and retention."
		/>
	);
}
